# Copy this file to .streamlit/secrets.toml for local runs (that file is
# gitignored and will NOT be committed). For a Streamlit Community Cloud
# deployment, paste the same two lines into the app's Settings -> Secrets
# box instead of using a file at all.

GROQ_API_KEY = "your-groq-api-key-here"

# Optional — only needed if you want scores auto-posted to a Google Sheet
# via an Apps Script Web App. Leave blank/omit if you're only using the
# local Excel scoresheet.
GSHEETS_WEBHOOK_URL = "https://script.google.com/macros/s/XXXXXXXX/exec"
